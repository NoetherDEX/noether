{
  "name": "@noether/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end round-trip harness: a scripted trade through the gateway → market → vault → indexer → API read-back (P4-24 / audit-readiness integration test).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "e2e": "tsx src/roundtrip.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.0.0"
  }
}
